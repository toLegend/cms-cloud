spring:  application:    name: cms-budget  cloud:    config:      label: master      profile:  dev      uri: http://localhost:8888/      discovery:        enabled: true        serviceId: config-server#    stream:#      kafka:#        bootstrap-servers: localhost:9092#    bus:#      enabled: true#management:#  endpoints:#    web:#      exposure:#        include: '*'management:  security:    enabled: false