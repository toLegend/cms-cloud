spring:  application:    name: cms-setting  cloud:    config:      label: master      profile:  pro      uri: http://localhost:8888/      discovery:        enabled: true        serviceId: config-server#    stream:#      kafka:#        bootstrap-servers: localhost:9092#    bus:#      enabled: true#management:#  endpoints:#    web:#      exposure:#        include: '*'management:  security:    enabled: false